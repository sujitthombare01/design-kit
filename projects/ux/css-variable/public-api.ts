/*
 * Public API Surface of @design-kit/ux/css-variable
 *
 * This entry point has no runtime TypeScript exports. It ships CSS custom
 * property definitions as static assets — consumers import the stylesheet
 * directly (see README.md), never a TypeScript symbol from this file.
 */
export {};
