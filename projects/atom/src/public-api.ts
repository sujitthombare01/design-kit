/*
 * Public API Surface of @design-kit/atom
 *
 * This primary entry point intentionally exports nothing. Every component in
 * this level package is consumed through its own secondary entry point
 * (e.g. `@design-kit/atom/button`, `@design-kit/atom/input`) so that
 * importing one Atom never pulls in another. See docs/02-architecture.md.
 */
export {};
